import React from 'react';
import { StyleSheet, View } from 'react-native';
import { Text } from 'react-native-elements';

const MissionInfos = () => {
    return(
        <View>
            <Text>Mission Infos</Text>
        </View>
    )
}

const styles = StyleSheet.create({})

export default MissionInfos